<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PokemonCardCatalogue.Pages.CollectionSetsPage"
             xmlns:pages="clr-namespace:PokemonCardCatalogue.Pages"
             xmlns:views="clr-namespace:PokemonCardCatalogue.Views.Sets"
             x:Name="ThisPage">
    <Shell.TitleView>
        <ContentView HorizontalOptions="Fill"
                     VerticalOptions="Fill">
            <Label Text="{Binding Title}"
               Style="{StaticResource LargeTitleViewLabelStyle}"/>
        </ContentView>
    </Shell.TitleView>
    <pages:BaseContentPage.Content>
        <RefreshView Command="{Binding RefreshCommand}"
                     IsRefreshing="{Binding IsLoading}">
            <CollectionView ItemsSource="{Binding SetItems}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical"
                                   ItemSpacing="0"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <views:AllSetsCardView BindingContext="{Binding .}"/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </pages:BaseContentPage.Content>
</pages:BaseContentPage>